# This file is kept for reference only
# All services are now deployed on k3d Kubernetes cluster
# To start the application:
# 1. Run: .\scripts\install-local-k3d.ps1
# 2. Access at: http://localhost

version: '3.8'
services:
  # ML API is now deployed on k3d cluster
  # Use: kubectl port-forward svc/ml-api 8000:8000
  # Or access through API Gateway at: http://localhost/api/risk
  placeholder:
    image: alpine
    entrypoint: echo "All services are running on k3d. See deploy/k8s/ for manifests."

